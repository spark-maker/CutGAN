1. python3.7
   pytorch
   pip install -r requirements.txt
2. Move 3 large files to the specified location in the weight folder
3. The images to be translated are placed in a folder in the dataset folder, for example："dataset/ab"
   run preprocess.py
4. run test.py,The generated image will be placed in the snapshot folder