1. python3.7
   pytorch
   pip install -r requirements.txt
2. The images to be translated are placed in a folder in the dataset folder, for example："dataset/ab"
   run preprocess.py
   The processed photos can be used for GAN testing.  for example："dataset/new"
3. run test.py,The generated image will be placed in the snapshot folder